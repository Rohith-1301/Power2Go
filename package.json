{
  "name": "power2go-mono",
  "version": "1.0.0",
  "description": "Monorepo root for Power2Go EV Portal serving static Next.js export",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "start": "node backend/dist/server.js"
  }
}
